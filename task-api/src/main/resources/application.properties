# MongoDB Configuration - Using separate properties to ensure no extra spaces in the database name
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=kaiburr-tasks

# Add a configuration for the server port
server.port=8081